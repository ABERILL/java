public enum Marka {
    A8(Model.АУДИ),
    ФОКУС(Model.ФОРД),
    АККОРД(Model.ХОНДА),
    XJ(Model.ЯГУАР),
    ПАТРОЛ(Model.НИСАН);

    public Enum name;

    Marka(Enum name) {
        this.name = name;
    }

}
